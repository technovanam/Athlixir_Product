# Fast-path limits — target ~5s end-to-end for metrics (overlay may finish shortly after)

MAX_ANALYSIS_SECONDS = 5
MAX_FRAME_WIDTH = 640
DOWNLOAD_CHUNK_SIZE = 65536

# Stabilization & Debug Engine
DEBUG_MODE = True
MIN_REQUIRED_FPS = 60
TARGET_FPS = 120
